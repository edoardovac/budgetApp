export const selectAllExpenseQuery = () => {
  return `SELECT * FROM Expense;`;
};

// need select by category
// need select by type (CASH, CARD, ecc...)
// need select by date (month, week, day, year?)
